<nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">

  <a class="navbar-brand" href='/home'</a>

  <ul class="navbar-nav ml-auto">
  <% if logged_in? %>
    <li class="nav-item align-self-center mr-3">
      <span class="navbar-link align-self-center">
      <%= link_to 'Membre du private Club', users_url %>
      </span>
    </li>
         <li class="nav-item mr-3">
      <%= link_to 'Ma page', user_path(session[:user_id]), {class: "btn btn-outline-primary"} %>
    </li>
    <li class="nav-item">
      <%= link_to 'Logout', session_path(session[:user_id]), {method: 'delete', class: "btn btn-outline-primary"} %>
    </li>
  <% else %>
    <%= link_to 'Login', new_session_path, {class: "login btn btn-outline-primary my-2 my-sm-0"} %>
  <% end %>
  </ul>

</nav>